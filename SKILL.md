---
name: exploded-view-web
description: 制作可交互的三维结构拆解网页、装配爆炸图和零件清单，或复刻 Human Atlas 的组装到分组散开再到零件排布效果。提供可运行模板、模型契约、GPU 批处理参考和逐步验收，适合按明确步骤执行的模型；不用于火焰粒子爆炸、破碎模拟或从单张图片自动推断真实内部结构。
---

# 三维结构拆解网页

交付能运行、能组装复原、能选择和筛选零件的网页。弱模型优先复制本技能已经提供的项目，再修改数据与样式；不要从概念描述重写整套引擎。

## 先选路径

| 用户目标 | 路径 | 第一个动作 |
|---|---|---|
| 没有提供模型，或希望先得到一个完整可运行的房屋示例 | `house` | 复制 `assets/house/`，先读它的 README 和资产说明 |
| 复现链接中的人体页面、保留 2,234 个部件 | `human-atlas` | 读 [逐步执行](references/replication-runbook.md) 的 A 路径 |
| 其他产品、机械、建筑、教学结构，或还没有模型 | `starter` | 读同文档 B 路径，先运行 12 零件示例 |
| 把现有效果接进已有项目 | 按现有框架集成 | 先读 [资产与接口契约](references/asset-contract.md)，沿现有入口挂载 |
| 仅分析参考仓库 | 分析 | 读 [原仓库实现分析](references/human-atlas-analysis.md)，不创建页面 |

用户明确目标优先；没有目标资产时先做明确标注的演示。需要真实外形、内部结构时，询问是否有可拆分模型；一体 Mesh、照片、概念名称不能证明内部零件几何。不要把示意模型称为真实复刻。

### 默认房屋数据

用户没有提供建模文件时，默认使用包内 `assets/house/` 的“胡桃木之家”房屋数据生成拆解网页；只有用户明确要求通用空白示例、其他主题或不公开房屋数据时才使用 `starter`。该数据包含可直接运行的静态 GLB、分组清单、材质贴图、夜景参考图和对应的 Blender 源模型。它是概念装修模型，不是实测结构、施工图或真实房屋内部结构推断；不要据此生成未建模的管线、承重判断或拆墙结论。

## 技能目录定位与运行

把本 `SKILL.md` 所在目录当作 `SKILL_DIR`。命令中的 `<SKILL_DIR>` 和 `<PROJECT_DIR>` 必须先替换成真实路径，不要原样执行尖括号。

```bash
python3 <SKILL_DIR>/scripts/create_project.py <PROJECT_DIR>
cd <PROJECT_DIR>
npm ci
npm test
npm run build
npm run dev
```

生成器默认复制房屋拆解示例。需要通用 12 零件模板时显式使用 `--mode starter`；需要固定的人体参考实现时使用 `--mode human-atlas`。

环境：Python 3.9+，Node.js 22.13+。Node 24 已验证。已有项目先看自身版本和规范；不在已有目录直接运行生成器。生成器只接受不存在或空目录，不覆盖用户文件。首次安装依赖需要网络，技能源码不依赖私有服务或 API key。

## 实施顺序

1. **跑基线。** 严格按对应路径启动；先确认加载完成、0%/45%/100%、搜索和隔离能工作。不要同时改依赖、资产和动画。
2. **接数据。** 读 [资产契约](references/asset-contract.md)，确认独立零件、唯一 ID、统一坐标和单位、静态变换、类别表、包围盒以及模型来源。通用模板自带静态 GLB 导入模块。
3. **接交互。** 保持 `visible/selected/isolate/explode` 单一状态来源，按 [逐步执行](references/replication-runbook.md) 的状态转移表实现。
4. **调视觉。** 稳定后再改名称、颜色和面板。使用可用画布矩形计算相机，100% 保持正视和平移。不要为了布局改变原始零件比例。
5. **按测量扩容。** 数千个不同几何零件需要读 [GPU 批处理](references/gpu-and-picking.md)，直接沿固定版本源码改。小模型沿用独立 Mesh；数量只是选型线索，不是性能保证。
6. **验收再交付。** 执行 [验收与故障表](references/acceptance-and-debugging.md)。记录命令、结果、截图和未验证项；构建通过不等于点选正确或手机不卡。

## 不能破坏的约束

- 这是“平移已有零件”，不是物理碰撞、拓扑切割或粒子模拟；不会自动生成缺失零件。
- 保存原始几何和基准变换；每帧从基准计算位移。禁止对上帧 position 持续 `+=`。
- 同一个零件的模型、边界、GPU 位移、CPU 拾取、标记、隔离相机必须使用同一坐标系和当前位移。
- 只对实际可见零件排布。普通模式：类别可见 **或** 被选中；隔离模式：只看被选中。
- 排布保证的是固定正视方向的二维包围盒空间分离；并不保证中间动画无穿插或任意旋转下没有遮挡。
- 目录概念可以包含多个零件；不要为了概念分组复制重复几何。
- 所有复用的上游代码保留 MIT 声明。人体模型另有 CC BY 4.0 归属，保留模型来源和改动说明。

## 包内资源

- `assets/house/`：默认房屋拆解示例，包含 1,296 个网格、77 个家具／构件、10 个空间、静态 GLB、分组清单、材质贴图、夜景参考图和公开的 Blender 源模型；数据边界见其中的 `HOUSE-DATA-NOTICE.md`。
- `assets/starter/`：原创通用示例，Three.js 0.159.0 + Vite 8.2.2；包含数学测试与静态 GLB 适配器。
- `assets/human-atlas/`：上游提交 `7a383d3ee2759e3ddf157c704fb8814fd0c50bcb` 的完整文本源码与模型目录。没有几何 `.bin/.gz`，用恢复脚本下载。
- `scripts/restore_models.py`：固定提交下载、Git blob 校验、解压校验；3 路并发，最多重试 3 次，不覆盖不同内容。
- `assets/upstream-lock.json`：上游提交、几何长度与哈希。
- `scripts/validate_bundle.py`：本地技能完整性验证。
- [可复制任务提示](references/task-prompts.md)：给弱模型的分段任务与明确完成条件。

原仓库保留了历史依赖，2026-09-06 审计出现 11 项依赖问题。`human-atlas` 用于固定基线复现，正式发布前在独立副本处理依赖并重跑交互回归；不要静默升级本技能的固定源码。具体边界见分析文档。

分发和署名详情见 [来源说明](references/NOTICE.md)；本技能的源码快照与示例可随整个目录一起转移。
